// 导出组件
export { default as Scrollbar } from './Scrollbar.vue'

// 导出类型
export type {
  ScrollbarEvents,
  ScrollbarInstance,
  ScrollbarPosition,
  ScrollbarProps,
  ScrollbarSize,
  ScrollbarSlots,
} from './types'
