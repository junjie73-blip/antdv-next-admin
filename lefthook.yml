pre-commit:
  parallel: true
  commands:
    oxc:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: pnpm run format
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    build:
      run: pnpm run build
